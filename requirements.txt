pyahocorasick>=2.1.0
# 二维码解码审核 (qrcode_decode_enabled) 所需，默认安装以免用户不会配置。
# headless 版无 GUI/系统依赖，纯 pip 跨平台安装，体积较小。
opencv-python-headless>=4.5.0
numpy>=1.21.0
# 本地 OCR 引擎（ocr_engine=local/auto 时使用，低配/离线友好，不依赖云端视觉 API）
# 缺失时自动回退云端视觉（llm 引擎），安装：pip install rapidocr_onnxruntime
rapidocr_onnxruntime>=1.3.24
